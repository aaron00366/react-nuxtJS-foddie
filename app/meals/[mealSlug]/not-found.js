export default function NotFoundPage() {
    return (
        <div className="not-found">
            <h1>Meal not found</h1>
            <p>Unfortunately, we could not find the requested page or requested meal data.</p>
        </div>
    );
}